doctype html

html
  head
    meta(charset='utf8')
    title 사용자 리스트 페이지
  body
    h1 사용자 리스트
    div
      ul
        - for (var i = 0; i < results.length; i++) {
        -  var curId = results[i]._doc.id;
        -  var curName = results[i]._doc.name;
        li #{i} - 아이디 : #{curId}, 이름 : #{curName}
        - }	 
    br
    br
    a(href='/public/adduser2.html') 추가하기
